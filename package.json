{
  "name": "iunu",
  "version": "1.0.0",
  "description": "chart data",
  "main": "./bin/www",
  "scripts": {
    "start": "node ./bin/www",
    "test": "echo \"Error: no test specified\" && exit 1",
    "seed": "mongoimport --db iunu --collection sensors --drop --file ./db/sensordata.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.15.3",
    "monk": "^5.0.2"
  },
  "devDependencies": {
    "morgan": "^1.8.2",
    "nodemon": "^1.11.0"
  }
}
